{
	"name": "bun-sendmail2",
	"version": "0.1.0",
	"scripts": {
		"test": "bun test test/*.test.ts",
		"ex1": "bun src/index.ts",
		"format": "oxfmt",
		"lint": "oxlint",
		"lint-tsc": "tsc --noEmit",
		"check": "oxfmt && oxlint --fix"
	},
	"dependencies": {
		"emailjs": "^5.0.2"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14"
	}
}
